# could import wrapper and provide shorter names for some constants,
# e.g. NCOPTION_NO_ALTERNATE_SCREEN -> NoAlternateScreen

# would need to import/export common from e.g. notcurses.nim and notcurses/core.nim
